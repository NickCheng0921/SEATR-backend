To run app
 - open command prompt in this directory "~/SEATR"
 - run command "python -m flask run"
 - you may have missing libraries, download w pip accordingly
    - alternatively, download all of them at once with command "pip download -r requirements.txt"

